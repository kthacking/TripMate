.lightbox-media {
    max-width: 100%;
    max-height: 85vh;
    border-radius: 8px;
    box-shadow: 0 20px 50px rgba(0,0,0,0.5);
    object-fit: contain;
    transition: opacity 0.3s ease;
}
video.lightbox-media {
    width: 80vw; /* Ensure video takes reasonable width */
    background: black;
}
